- category: oschecks
  type: fs_used
  interval: 300
  script: mon_scripts/df.sh
  shell: /usr/bin/bash
- category: oschecks
  type: swap_used
  interval: 300
  script: mon_scripts/swap.sh
  shell: /usr/bin/bash
- category: performance
  type: system
  interval: 1
  script: mon_scripts/perfstats.py
  shell: /usr/bin/python3
